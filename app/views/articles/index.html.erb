<div class="p-2">
  <h1 class="text-2xl text-yellow-500">Tinkering in Rails</h1>
  <p>A blog for learning and experimenting</p>
</div>
  <div class="container mt-28 px-5">
  <p class="text-slate-500 font-medium">This blog has <%= Article.public_count %> public articles and counting!</p>

  <div class="grid grid-cols-3 gap-8 mb-6">
    <% @articles.each do |article| %>
      <% unless article.archived? %>
        <div class="p-6 bg-white rounded-xl shadow-md">
          <%= link_to article.title, article %>
        </div>
      <% end %>
    <% end %>
  </div>

  <button class="p-2 bg-yellow-500 rounded shadow hover:shadow-lg"><%= link_to "New Article", new_article_path %></button>
</div>