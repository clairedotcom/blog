<div class="grid-cols-1">
  <div>
    <h1 class="text-2xl text-yellow-500">Articles</h1>
  </div>
  <p class="text-slate-500 font-medium">Our blog has <%= Article.public_count %> public articles and counting!</p>

  <ul>
    <% @articles.each do |article| %>
      <% unless article.archived? %>
        <li>
          <%= link_to article.title, article %>
        </li>
      <% end %>
    <% end %>
  </ul>

  <button class="bg-yellow-500 rounded"><%= link_to "New Article", new_article_path %></button>
</div>